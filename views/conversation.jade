extends layout

block content
  div(style='max-width: 642px; padding: 20px; margin:auto;')
    h1 Conversation Echo Server
    p
      | This is a simple echo server that takes the text input, sends it to the server.
      | The server then will respond, first to say when it will echo back the text
      | and then after that period has elapsed will echo the actual text back.
    p
      | Note that the messages are returned by a random time delay and that effects the
      | returned order
  form(onsubmit='return false')
    #conversation(name='conversation', style='overflow-y:scroll; height:400px; border:1px solid black; padding:0px 20px 0px 20px; max-width:600px; margin:auto;')
    div(style='max-width:642px; margin:auto; padding: 20px 0px 20px 0px;')
      input#statement(style='width:80%;', type='text', name='statement')
      input#ask(style='width:15%;', type='button', name='ask', value='Ask')
  script(src='http://code.jquery.com/jquery-latest.min.js', type='text/javascript')
  script(src='/javascripts/longpoll.js', type='text/javascript')
